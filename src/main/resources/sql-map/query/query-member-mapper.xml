<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miner.pickax.login.mapper.MemberMapper">
	<select id="getUser" parameterType="com.miner.pickax.login.vo.UserVo" resultType="com.miner.pickax.login.vo.UserVo">
		SELECT * 
		FROM tb_member
		<where>
			<if test="userID !=null and userID !=''">
				userID = #{userID}
			</if>
			<if test="userPW !=null and userPW !=''">
				and userPW = #{userPW}
			</if>
			<if test="userType !=null and userType !=''">
				and userType = #{userType}
			</if>
		</where>
	</select>
	
	<insert id="setUser" parameterType="com.miner.pickax.login.vo.UserVo">
		INSERT INTO tb_member (userID, userPW, userEmail, phoneNumber, register_date,userName,userBirth,userAddr,userType)
		VALUES (#{userID}, #{userPW}, #{userEmail}, #{phoneNumber}, #{register_date}, #{userName}, #{userBirth}, #{userAddr},#{userType})
	
	</insert>
	
</mapper>